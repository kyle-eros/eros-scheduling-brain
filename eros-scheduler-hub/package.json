{
  "name": "eros-scheduler-hub",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc && mkdir -p dist/ui && cp -R src/ui/. dist/ui",
    "lint": "eslint --ext .ts src",
    "push": "npm run build && clasp push",
    "pull": "clasp pull"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2",
    "@types/google-apps-script": "^1.0.69",
    "eslint": "^8.56.0",
    "eslint-config-google": "^0.14.0",
    "typescript": "^5.4.0"
  }
}
